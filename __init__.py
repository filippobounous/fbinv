"""Top-level package for the personal project.

This package exposes two FastAPI applications located under the
``api`` package. Launch them with ``uvicorn`` from their respective
modules::

    uvicorn api.investment.main:app --reload
    uvicorn api.inventory.main:app --reload
"""

